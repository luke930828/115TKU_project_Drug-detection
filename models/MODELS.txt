# 模型權重清單
#
# 格式：檔名|下載網址|SHA256|說明
#
# 權重檔本身不進 git，只有這份清單進。
# 換模型時改這裡的網址和 SHA256，程式碼不用動。
#
# 上傳流程：
#   1. 到 GitHub Releases 頁面，Draft a new release
#   2. 把 best.pt 拖進 Attach binaries
#   3. sha256sum best.pt 拿到雜湊值，貼到下面
#   4. 發布後複製檔案網址貼到下面

best.pt|https://github.com/luke930828/115TKU_project_Drug-detection/releases/download/v1.0.0/best.pt|cef6b020ea2fd26702ecf65f7d1e82551e4f2ab2bfc24c61a6ceab92740fd263|YOLO 主模型 v3（15 類，自訓練。拿掉 medical_bottle；驗證集 mAP50 0.600、precision 0.683）
yolo11n.pt|https://github.com/luke930828/115TKU_project_Drug-detection/releases/download/v1.0.0/yolo11n.pt|0ebbc80d4a7680d14987a577cd21342b65ecfd94632bd9a8da63ae6417644ee1|YOLO11 nano 官方預訓練權重（ultralytics 依賴，非自訓練）

# 網址還沒真的建過 Release，現在是佔位——上傳流程照上面第 1-4 步做完，
# 網址就會是對的；SHA256 已經照本機這兩個檔案算好了，不用重算。
#
# 2026-09-10：repo 從 114TKU 改名為 115TKU，網址一併更新。
# 2026-09-10：主模型換成 v3（15 類）。舊的 16 類版本留在 models/best_v1.pt 與
#            modules/yolo/app/models/best_v1_backup.pt，要回滾直接換回來即可，
#            但 scoring.py 的權重表要一起換回 16 類版本——兩者是綁在一起的。
# GitHub 會自動轉址所以舊網址暫時還通，但別依賴它。

# NLP 微調模型已經放在 Hugging Face Hub，不用透過這份清單下載，程式裡直接：
#   AutoModelForSequenceClassification.from_pretrained("matt0513/drug-detection-xlm-roberta")
# 比自己打包 tar 好管理，也有版本控制。
